ALI= Aldolase_C1_MAFFT.fas
PDBDIR=/data/ortizg/databases/pdb/ ! path to pdb files (Modify!)
PDBEXT=.pdb                        ! Extension of pdb files
#
NORM=MIN    ! Normalize seq.id, TM, Cont.overlap based on MIN MAX or MEAN length
ALI_SS=1    ! Correct multiple sequence alignment with secondary structure?
SS_MULT=1   ! Correction pairwise (0) or multiple (1)?
SHIFT_MAX=9 ! Maximum shift when correcting MSA with sec.str.
#
PRINT_SIM=1    ! Print structure similarity
PRINT_DIV=1    ! Compute and print divergence measures
PRINT_CV=0     ! Compute and print molecular Clock Violations
PRINT_PAIR=0   ! Print pairwise alignments that target PC_sim
PRINT_CLIQUE=1 ! Print MSA based on cliques of pairwise alignments targeting PC_sim
